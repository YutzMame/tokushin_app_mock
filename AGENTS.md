# 初期化と文脈管理(自動実行)
- タスク開始時、ルートに `yutz_local/` がなければ作成し、`.git/info/exclude` に未記載なら追記する。
- コンテキストは常に `yutz_local/MEMORY.md` で管理（開始時読込・完了時更新）する。
- 要件定義や個人的な設計メモは `yutz_local/docs/` に保存する。

# 共有・議事録
- 週次MTGの議事録にそのまま貼り付けられるよう、進捗や共有事項の出力が求められた際は箇条書きで簡潔にまとめる。
- 宛先の追加や特別なフォーマットは、別途指示があった場合のみ対応する。
